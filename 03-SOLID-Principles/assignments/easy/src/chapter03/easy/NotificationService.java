package chapter03.easy;

// TODO (SRP): This class ONLY coordinates sending — it doesn't log, format, or know types
// TODO (DIP): Take NotificationSender and Logger as constructor params (inject them)
// Method: void send(String recipient, String message)
//   → calls sender.send() then logger.log()

public class NotificationService {

}
