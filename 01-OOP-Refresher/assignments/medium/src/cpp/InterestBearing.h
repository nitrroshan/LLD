#pragma once

// TODO: Define this as a pure interface (abstract class with only pure virtual methods)
// Method: virtual void applyInterest() = 0

class InterestBearing {
public:
    virtual ~InterestBearing() = default;
};
